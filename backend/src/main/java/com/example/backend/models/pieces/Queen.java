package com.example.backend.models.pieces;

import com.example.backend.models.Color;

public class Queen extends Piece {
    public Queen(Color color, Integer position) {
        super(color, position);
    }
}
