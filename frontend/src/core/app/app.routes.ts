import { Routes } from '@angular/router';
import { BoardComponent } from '../../features/game/components/board/board.component';

export const routes: Routes = [{ path: 'play', component: BoardComponent }];
